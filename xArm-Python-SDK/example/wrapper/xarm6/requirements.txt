# Dependencies for haply_control.py
websockets>=11.0
scipy>=1.9
pybullet>=3.2

# For setup_urdf.py FT sensor mesh conversion (gltf → obj)
trimesh>=3.20

# The xArm Python SDK is the repo itself.
# From the repo root, install it once in editable mode:
#   pip install -e .
